import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  image: {
    maxWidth: '90%',
    borderRadius: '3px',
    objectFit: 'cover',
    boxShadow: '0.5em 0.5em 1em gray',
  },
}));
